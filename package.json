{
  "name": "glassbox-mc",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build:base": "tsc -b packages/base",
    "build:user": "tsc -b packages/user",
    "build:worker": "tsc -b packages/worker",
    "build:cassandra-web": "tsc -b packages/cassandra-web",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:user": "vitest",
    "build": "lerna run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^5.4.3",
    "vitest": "^0.22.1"
  },
  "dependencies": {
    "typescript": "^4.7.4"
  }
}
